PASS 123.42 — MOBILE MENU NATIVE TAP NAVIGATION

Fix target:
- Mobile menu opens, but tapping WORK / ABOUT / REVIEWS / CONTACT can appear to do nothing or leave the menu covering the destination.

Changes:
- Direct click listener is bound to every mobile nav anchor.
- Mobile nav click no longer cancels native fragment navigation with preventDefault().
- Menu closes directly from the tapped anchor.
- Hashchange independently closes any stuck-open menu.
- Same-hash taps get a fixed-header-aware scroll correction.
- Delegated click listener remains as a non-cancelling fallback.
- Navigation assets cache-busted to 123.42.
- Package version bumped to 9.13.42.
